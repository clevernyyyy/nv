import Ember from 'ember';

export default Ember.Route.extend({
    //   redirect: function () {
    //     this.transitionTo('login');
    // }

    // this doesn't work, figure out better way to redirect
    // 'will transition'  look up in ember api
    // transition.abort  and transition.retry

});
